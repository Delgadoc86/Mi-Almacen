Backup textual de las Firestore Security Rules activas en Firebase Console
(proyecto minegocio-8bbef), copiadas manualmente el 2026-07-05, ANTES del
primer deploy vía este repo. Sirve para poder revertir por copy/paste desde
la consola si algo sale mal con el deploy nuevo.

rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /users/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
    match /businesses/{businessId} {
      allow read, write: if request.auth != null && request.auth.uid == businessId;
      match /products/{productId} {
        allow read, write: if request.auth != null && request.auth.uid == businessId;
      }
      match /categories/{categoryId} {
        allow read, write: if request.auth != null && request.auth.uid == businessId;
      }
      match /customers/{customerId} {
        allow read, write: if request.auth != null && request.auth.uid == businessId;
        match /movements/{movementId} {
          allow read, write: if request.auth != null && request.auth.uid == businessId;
        }
      }
      match /cashSessions/{sessionId} {
        allow read, write: if request.auth != null && request.auth.uid == businessId;
        match /cashMovements/{movementId} {
          allow read, write: if request.auth != null && request.auth.uid == businessId;
        }
      }
    }
  }
}
